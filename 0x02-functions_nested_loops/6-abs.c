#include "main.h"

/**
 * _abs - in Main
 * @m - an integer output
 * Description - the function returns absolute value of a num
 *
 * Return: Always 0 (Success)
 */

int _abs(int m)
{
	if (m >= 0)
		return (m);
	else
		return (m * -1);
}
