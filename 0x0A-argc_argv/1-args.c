#include <stdio.h>

/**
 * main - prints number of arguemnets passed to it
 * @argc: arguement count
 * @argv: arguemet vector
 * Return: 0.
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}
